from setuptools import find_packages, setup


setup(
      name='git_lib',
      packages=find_packages(include=['git_lib']),
      version='0.1.0',
      description='Test for the usage of our libs within github',
      url='git@github.com:matheusAranha11/git_lib.git',
      author='Matheus Lopes',
      license='MIT',
      install_requires=[],
      zip_safe=False
      )

